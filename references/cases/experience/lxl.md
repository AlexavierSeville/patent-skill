# 撰写者经验档案 — lxl

> 撰写者：lxl　对应审稿人：王工
> 登记（自助注册，由本人首个 PR 填写、维护者合并即生效）：
> github: q1480371991
> writer_dir: 待本人填写（~/Desktop/Patent/<目录名>/）
> 条目模板与入档纪律见本目录 `README.md`；本文件由 lxl 通过 PR 维护，规则升格由仓库维护者执行。

## 审核偏好记录

| # | 偏好一句话 | 批注原文摘录（含锚点选区） | 含义解读（改前→改后 + 为什么） | 适用边界 | 升格状态 |
|---|---|---|---|---|---|
| 1 | 方法权 1 第一步一定是获取数据 | X2607134 权要审核1 批注0，锚「渠道表面连续视觉数据流」：「所有方法的第一步一定是获取数据，你这里的数据流是哪里来的呢，没有获取就没有来源」（2026-07-30） | 改前权1 首句直接对「连续视觉数据流」做处理→改后王工亲笔以「获取包含…的渠道表面图像」开端；无获取步骤则后续处理无来源（空中楼阁） | 全部方法独权，跨案通用 | 已升格 → `claims.md` L1-1 |
| 2 | 不写并列操作，每个子步骤独立输入输出 | X2607134 权要审核1 批注2/3，锚「融合后进行降噪与反光干扰区域重构」「多尺度特征提取与空间对齐叠加」：「不要去写并列的操作，每个子步骤要有独立的输入输出，要写清楚」（2026-07-30） | 改前一个动作短语里用「与」并列两个独立操作→改后一步一操作一输出逐段拆写；并列操作让输入输出对应不清 | 权1 分句与从权子步骤，跨案通用 | 已升格 → `claims.md` L1-1 |
| 3 | 概括操作要合理：动作与输出形态不得跳跃，步骤顺序满足数据依赖 | X2607134 权要审核1 批注1/5，锚「分离得到静态纹理矩阵与动态波纹矩阵」：「对数据流进行分离，怎么会得到矩阵，应该是根据分离的数据去构建矩阵，太跳跃了」「先有基准图才能去提取掩码，原权1写的这里逻辑跳跃了」（2026-07-30） | 改前「分离→矩阵」跨形态、掩码先于基准图→改后按数据依赖排序且概括动作直达输出（分离后的数据去构建矩阵） | 权要与说明书的上位概括处，跨案通用 | 已升格 → `claims.md` L1-1 |
| 4 | 从权只有三种形式，无「先写一段话再其中」的写法 | X2607134 权要审核1 权7 口头补充：「从权三种形式，解释权1中的一个子步骤，这是我们用的最多的；或者去解释一个特征的含义；第三种就是在步骤前后，补充操作过程…没有这种先写一段话，再其中，如何如何的写法」（2026-07-30） | 改前权7 先引一句再另立「其中，所述空间维度…包括：」→改后引导句整句连引权1 相邻动作、子步骤按顺序解释 | 全部从属权要，跨案通用 | 已升格 → `claims.md` L1-1 |
| 5 | 灰度化/色彩转换/对比度增强等基础手段权1 概括为「预处理」，细节下从权 | X2607134 权要审核1 批注5/6：「灰度化，色彩转换，对比度增强都是非常基础的图像处理手段，不需要写这么详细，简单预处理就概括了，可以再从权讲预处理是要先灰度化，然后再转换之类的」（2026-07-30） | 改前权1 逐一点名基础处理→改后权1 一句「进行预处理」、从权按灰度化→拉伸→滤波→重构逐步展开 | 图像/信号类方法权，跨案通用 | 已升格 → `claims.md` L1-1 |
| 6 | 创新块详写；从权解释到对应子步骤输出即止，执行流程清楚、适当概括 | X2607134 权要审核1 口头：「创新步骤和解决技术问题的步骤是要详细写的…权1梳理清楚后，从权就不要再写的太多太复杂，执行流程清楚，适当概括就好了」（2026-07-30） | 改前从权带条件分支/兜底/两级判据等复杂逻辑→改后删除分支兜底、支撑环节 2-4 步概括、聚焦创新块（权6/权7）保持精确链条 | 从权粒度取舍，跨案通用 | 已升格 → `claims.md` L1-2 |
| 7 | 处理链不得删除/破坏后续要识别的目标信号（信号保全）；疑似目标弱化而非抹除 | X2607134 定稿 批注0，锚权2「识别为反光干扰区域」：「萌芽期渗漏点恰恰属于'高亮区域（水光反射）'。预处理阶段将其直接判定为'干扰'并强行用周围背景像素'抹除/修复'掉了…后续的CNN、接缝掩码、特征聚焦都将面对一个'无渗漏信号'的虚假基准图」（2026-08-03） | 改前权2把高亮反光区域插值重构（抹除）→改后帧间时序差分区分动态水面波纹与静态渗漏、渗漏候选区保留原值。为什么：高亮水光正是渗漏信号，预处理删了=权1后续识别全建立在无信号虚假图上 | 所有含预处理/筛选/过滤/重构步骤的方法权，跨案通用 | 待维护者升格 →（建议 `global.md` G3-1 新增「信号保全」条；本轮规则文件改动因分支保护被回退，由维护者升格） |
| 8 | 动态干扰 vs 静态目标可用帧间时序差分做动静分离（水利视觉检测黄金准则） | X2607134 定稿 批注1：「水面波纹是动态的（随时间变化），而接缝渗漏的水光是静态的（固定在接缝位置）。利用'帧间差分'天然就能区分二者…这种'动/静分离'的预处理，相对于您原来'一刀切插值'的方法，具有极高的创造性和技术贡献度」（2026-08-03） | 改前空间形态一刀切（高亮即干扰）→改后按时间维度区分：波动=波纹干扰、固定=渗漏信号。干扰与目标在时间维度可分时，时序差分优于空间形态分类 | 图像/视频监测类方法权（水面波纹、背景运动等场景） | 待升格（案例级） |
| 9 | 直线/边缘聚类合并须有空间位置+端点距离双约束，不能只看角度方向 | X2607134 定稿 批注2，锚权3「将倾角落入同一预设角度区间聚为一类」：「聚类只看'角度'，合并只看'方向'，缺少'空间位置'和'端点距离'的双重约束，会导致不同位置平行接缝被错误聚拢、远距离断裂线段被强行拼凑…通过'形态学膨胀+空间连通+取并集'彻底解决」（2026-08-03） | 改前角度区间聚类+共线合并→改后倾角容差分类→二值掩模+形态学膨胀使共线断裂连通→直线拟合+取并集。空间连通约束让"同一方向"的边线按真实位置聚拢，避免跨位置错聚 | 直线/边缘聚合、接缝/轨迹提取类步骤 | 待升格（案例级） |
| 10 | 掩码类加权不得粗暴置0破坏下游卷积/归一化输入（感受野需上下文） | X2607134 定稿 批注3，锚权7「将…特征值置为0」：「CNN的卷积核在滑动计算时，需要感受野内包含上下文背景信息…掩码外区域全部粗暴置0…边缘处局部响应归一化的分母畸变（窗口均值严重偏低）…产生'边缘伪响应'，反而引入新的误报。改为弱化抑制，而非彻底抹除」（2026-08-03） | 改前掩码外置0→改后弱化抑制（保留上下文衰减）。下游是卷积+归一化时，置0制造大量0值窗口→窗口均值畸变→边缘值被放大成伪响应 | 掩码/注意力加权结果进入卷积或归一化类算子时，跨案通用 | 待维护者升格 →（并入「信号保全」条，同第 7 条） |
| 11 | 判别量统计口径匹配目标/噪声形态：连续渐变用和值均值、孤立尖刺用极值会误判 | X2607134 定稿 批注4/5，锚权8「差值绝对值的最大值」：「真实渗漏的渐变区域其极值可能并不突出（最尖峰反而在过渡带上），而均值类统计更能稳定表征区域整体异常程度」「响应连续异常区域，对孤立噪声抑制好」（2026-08-03） | 改前"绝对值最大值"（单方向大差值即高响应，被混凝土刻痕等孤立噪声劫持）→改后"所有差值绝对值之和"（对连续渐变区域整体异常敏感、抑制孤立尖刺）。判别统计量的口径须匹配目标信号的真实形态 | 任何"从邻域/集合统计出判别量"的步骤，跨案通用 | 待维护者升格 →（建议 `claims.md` L1-1 新增「从权子步骤合理性推演」条并含统计口径分项） |
| 12 | 权要根据背景技术和权1的逻辑去分析，步骤是否合理 | X2607134 定稿 王工口头：「权要要根据背景技术和权1的逻辑去分析，步骤是否合理」（2026-08-03） | 方法：写每条从权子步骤时回到背景技术描述的渗漏形态（待识别对象长什么样）+权1整体信号流（每步输入输出、下游怎么用）推演——这步会不会误删目标信号（批注0）、会不会让下游算子失真（批注3）、统计口径是否匹配判别形态（批注4） | 权要撰写/返修全程，跨案通用 | 待维护者升格 →（建议 `claims.md` L1-1 新增「从权子步骤合理性推演」条，含回背景技术+权1信号流两对照点） |
| 13 | 阈值设定必须给具体操作过程（先干嘛→再干嘛→选什么值），不得概括一句"根据a和b确定" | X2607134 全文1稿审核v1 王工口头补充：「阈值的设定不能概括一句，比如说：xx阈值是根据a和b确定的，是这么写还是不清楚如何根据ab确定，要有具体的操作过程，先干嘛，在干嘛，最后选什么值作为阈值」；配套23条批注中约10条直指阈值笼统（批注3/4/7/8/11/12/19/21） | 改前各处"按实验统计方式确定，取XX"或"根据ab确定"→改后完整操作链：采集什么样本/覆盖什么工况→做什么统计动作（排序/取第几百分位/求分布中点/网格搜索）→选哪个值→为什么该样本量足够（尾部稳定性验证）。三类设定方式：公知常识（领域已知范围+示例值）、实验统计（样本+统计动作+分位）、算法动态（Otsu/网格搜索/累计贡献率，含未检出时兜底） | 全部阈值/参数/系数，跨案通用 | 待维护者升格 →（建议 `global.md` G6-1 阈值确定依据条加"必须给操作过程+三类方式"分项；本轮规则文件改动因分支保护被回退） |
| 14 | 说明书解释段不用冒号引导（名词定义式与"XX确定："），解释直接跟在特征名词后 | X2607134 全文1稿审核v1 批注6/18：「不要用冒号，具体解释直接跟在对应特征名词后面就好了」「检查全文，后续撰写不要用冒号来引导了」 | 改前"…自身：外接矩形长宽比…"、"…方式确定：采集…"→改后冒号改逗号或直接连接。合法保留：权要/发明内容"包括："与"有益效果："格式引导 | 说明书解释段，跨案通用 | 待维护者升格 →（建议 `full-draft.md` L8-1 解释话术条加"解释段不用冒号引导"分项） |
